# YouTubePy

Simple command-line app to download a YouTube video.

## Setup

```powershell
.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

## Run

```powershell
python main.py
```

Paste a YouTube video URL when prompted. The video will be downloaded to:

`D:/Video Capture/Youtube Videos`
